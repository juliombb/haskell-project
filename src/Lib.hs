module Lib where
--module Lib ( Point, dist ) where

--import Data.List
--
--
--data Point = Point Double Double
--
--dist (Point xa ya) (Point xb yb) = sqrt (sq (xa - xb) - sq (ya-yb))
--
--sq a = a * a